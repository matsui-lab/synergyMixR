% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_k_selection_comparison.R
\name{run_k_selection_comparison}
\alias{run_k_selection_comparison}
\title{Run K Selection Method Comparison (v3)}
\usage{
run_k_selection_comparison(
  test = FALSE,
  cores = 4,
  output_dir = NULL,
  parallel_strategy = c("local", "pbs", "future"),
  pbs_config = list(),
  pbs_submit = FALSE,
  pbs_wait = FALSE,
  Kvec = 2:6,
  r_fixed = 2,
  max_iter = 50,
  verbose = TRUE
)
}
\arguments{
\item{test}{Logical. If TRUE, use smaller test grid. Default FALSE.}

\item{cores}{Integer. Number of CPU cores. Default 4.}

\item{output_dir}{Character. Output directory. Default auto-detected.}

\item{parallel_strategy}{Character. "local", "pbs", or "future".}

\item{pbs_config}{List. PBS configuration (queue, walltime, mem, r_module).}

\item{pbs_submit}{Logical. If TRUE, submit PBS job. Default FALSE (dry run).}

\item{pbs_wait}{Logical. If TRUE, wait for PBS completion. Default FALSE.}

\item{Kvec}{Integer vector. Candidate K values. Default 2:6.}

\item{r_fixed}{Integer. Fixed r value for two-stage. Default 2.}

\item{max_iter}{Integer. Maximum EM iterations. Default 50.}

\item{verbose}{Logical. Print progress. Default TRUE.}
}
\value{
Data frame with results, or PBS job info if using PBS.
}
\description{
Compares multiple K selection methods (BIC, ICL with various temperatures,
Ensemble, Adaptive, Silhouette) across a parameter grid.
}
\details{
This function compares the following K selection methods:
\itemize{
  \item BIC: Bayesian Information Criterion
  \item ICL_T1: ICL with temperature = 1.0
  \item ICL_T2: ICL with temperature = 2.0 (recommended)
  \item ICL_T3: ICL with temperature = 3.0
  \item Ensemble: Majority vote of BIC, ICL_T1, ICL_T2
  \item Adaptive: Adaptive temperature selection
  \item Silhouette: Silhouette-based selection
}
}
\examples{
\dontrun{
# Local test run
results <- run_k_selection_comparison(test = TRUE, cores = 2)

# Full simulation
results <- run_k_selection_comparison(test = FALSE, cores = 8)

# PBS submission
run_k_selection_comparison(
  test = FALSE,
  parallel_strategy = "pbs",
  pbs_config = list(queue = "batch"),
  pbs_submit = TRUE
)
}

}
