% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pbs_utils.R
\name{run_sweep_future}
\alias{run_sweep_future}
\title{Run Parameter Sweep Using Future}
\usage{
run_sweep_future(
  param_grid,
  run_func,
  ...,
  future_backend = "multisession",
  workers = NULL,
  resources = list(),
  chunk_size = NULL,
  verbose = TRUE
)
}
\arguments{
\item{param_grid}{Data frame. Parameter grid to sweep.}

\item{run_func}{Function. Function to run for each parameter combination.
Should accept a single row of param_grid and return a data frame.}

\item{...}{Additional arguments passed to run_func.}

\item{future_backend}{Character. Backend to use (see \code{\link{setup_future_backend}}).}

\item{workers}{Integer. Number of workers for local backends.}

\item{resources}{List. HPC resources for batchtools backends.}

\item{chunk_size}{Integer. Number of tasks per future (for load balancing).}

\item{verbose}{Logical. Print progress information.}
}
\value{
Data frame with combined results from all parameter combinations.
}
\description{
Executes a parameter sweep using the future framework for parallelization.
This works with any future backend (local, PBS, SLURM, etc.).
}
\examples{
\dontrun{
# Define a simple run function
my_run_func <- function(p) {
  Sys.sleep(1)  # Simulate work
  data.frame(run_id = p$run_id, result = p$x^2)
}

# Create parameter grid
param_grid <- data.frame(run_id = 1:10, x = 1:10)

# Run with local parallelization
results <- run_sweep_future(
  param_grid, my_run_func,
  future_backend = "multisession",
  workers = 2
)

# Run with PBS
results <- run_sweep_future(
  param_grid, my_run_func,
  future_backend = "batchtools_torque",
  resources = list(walltime = "01:00:00")
)
}

}
