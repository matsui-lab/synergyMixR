% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{select_r_by_vaf_mpca}
\alias{select_r_by_vaf_mpca}
\title{Select Minimum r Based on VAF Threshold for MPCA}
\usage{
select_r_by_vaf_mpca(
  list_of_data,
  K,
  rvec = 1:5,
  vaf_threshold = 0.9,
  max_iter = 50,
  nIterPCA = 5,
  tol = 0.001,
  method = "EM",
  n_init = 1,
  use_kmeans_init = TRUE,
  subject_rdim_for_kmeans = 2,
  mc_cores = 1,
  n_threads = 1
)
}
\arguments{
\item{list_of_data}{A list of data matrices, each (T_i x M).}

\item{K}{Number of clusters (must be pre-specified).}

\item{rvec}{Vector of candidate r values to test.}

\item{vaf_threshold}{Target VAF threshold (default: 0.90 for 90 percent).}

\item{max_iter}{Maximum EM iterations.}

\item{nIterPCA}{Sub-iterations within the PCA/PPCA update.}

\item{tol}{Convergence tolerance.}

\item{method}{"EM" or "closed_form".}

\item{n_init}{Number of random initializations.}

\item{use_kmeans_init}{Whether to use k-means initialization.}

\item{subject_rdim_for_kmeans}{Dimension for PCA-based features.}

\item{mc_cores}{Number of CPU cores for parallelization.}

\item{n_threads}{Number of OpenMP threads.}
}
\value{
A list containing: summary (data frame with r, VAF, BIC for each r),
  best_r_vaf (minimum r achieving VAF threshold), best_r_bic (optimal r by BIC),
  best_model_vaf (model with minimum r achieving threshold), and
  best_model_bic (model with optimal BIC).
}
\description{
This function implements a traditional EMG synergy approach: select the minimum
r (number of principal components) that achieves a target VAF threshold.
This is commonly used in motor control research where VAF >= 90-95 percent is desired.
}
\details{
This function fits MPCA models for each r in rvec with fixed K, computes both
VAF and BIC, and returns:
1. The minimum r that achieves the VAF threshold (traditional approach)
2. The optimal r by BIC (statistical approach)

Users can compare both criteria to make informed model selection decisions.
}
