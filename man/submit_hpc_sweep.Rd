% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pbs_utils.R
\name{submit_hpc_sweep}
\alias{submit_hpc_sweep}
\title{Submit Parameter Sweep to HPC (Simple Interface)}
\usage{
submit_hpc_sweep(
  cluster_type = c("local", "pbs", "slurm", "sge"),
  queue = NULL,
  walltime = "04:00:00",
  memory = NULL,
  test = FALSE,
  cores = 4,
  output_dir = NULL,
  submit = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{cluster_type}{Character. Type of HPC cluster:
\itemize{
  \item "local": Local multicore execution (no HPC)
  \item "pbs": PBS/OpenPBS/TORQUE cluster
  \item "slurm": SLURM cluster
  \item "sge": SGE cluster
}}

\item{queue}{Character. Queue/partition name for the cluster. Required for
HPC clusters.}

\item{walltime}{Character. Maximum wall time per job (default: "04:00:00").}

\item{memory}{Character. Memory per job (default: "4gb" for PBS, "4096" for SLURM).}

\item{test}{Logical. If TRUE, use test parameter grid. Default is FALSE.}

\item{cores}{Integer. Number of cores for local execution. Default is 4.}

\item{output_dir}{Character. Output directory for results.}

\item{submit}{Logical. If TRUE, submit jobs. If FALSE, just generate scripts.
Default is TRUE.}

\item{verbose}{Logical. Print progress messages. Default is TRUE.}
}
\value{
Invisibly returns the result from \code{run_param_sweep_modelsel}.
}
\description{
A simplified wrapper for submitting parameter sweeps to HPC clusters.
Automatically selects the appropriate parallelization strategy based on
the specified cluster type.
}
\details{
This function provides a simplified interface for the most common HPC use cases.
It automatically:
\itemize{
  \item Selects the appropriate future backend for the cluster type
  \item Configures resource specifications
  \item Handles PBS vs SLURM differences
}

For more control over the submission, use \code{\link{run_param_sweep_modelsel}}
directly with \code{parallel_strategy = "future"} or \code{"pbs"}.
}
\examples{
\dontrun{
# Local execution (8 cores)
submit_hpc_sweep("local", cores = 8, test = TRUE)

# PBS cluster
submit_hpc_sweep("pbs", queue = "batch", walltime = "02:00:00")

# SLURM cluster
submit_hpc_sweep("slurm", queue = "compute", memory = "8192")

# Dry run (generate scripts without submitting)
submit_hpc_sweep("pbs", queue = "batch", submit = FALSE)
}

}
\seealso{
\code{\link{run_param_sweep_modelsel}}, \code{\link{setup_future_backend}}
}
