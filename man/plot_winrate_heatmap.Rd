% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sweep_results.R
\name{plot_winrate_heatmap}
\alias{plot_winrate_heatmap}
\title{Plot Win Rate Heatmap for Parameter Sweep}
\usage{
plot_winrate_heatmap(win_rates_df, facet_by = "metric")
}
\arguments{
\item{win_rates_df}{Data frame with win rates from \code{run_param_sweep()}.
Must contain columns: Method, model_type, metric, win_rate.
This is typically the data from the win_rates.csv file.}

\item{facet_by}{Character. Variable to facet by. One of "model_type" or "metric".
Default is "metric".}
}
\value{
A ggplot2 object
}
\description{
Creates a heatmap showing the percentage of parameter combinations where
each method performed best for each metric.
}
\examples{
\dontrun{
library(synergyMixR)
results <- run_param_sweep(test = TRUE, cores = 2)

# Read win rates
win_rates <- read.csv(results$win_rates_file)

# Plot win rate heatmap
p <- plot_winrate_heatmap(win_rates)
print(p)
}

}
