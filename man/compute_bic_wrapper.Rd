% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation_wrappers.R
\name{compute_bic_wrapper}
\alias{compute_bic_wrapper}
\title{Compute BIC for a Fitted Model}
\usage{
compute_bic_wrapper(method, fit, K, r, M, N_obs)
}
\arguments{
\item{method}{Model type: "MFA" or "MPCA".}

\item{fit}{A fitted model object.}

\item{K}{Number of clusters.}

\item{r}{Factor dimension.}

\item{M}{Number of observed dimensions (muscles).}

\item{N_obs}{Number of observations (typically sum of T_i).}
}
\value{
Numeric scalar: BIC value. Lower is better. Returns NA if
  computation fails.
}
\description{
Computes the Bayesian Information Criterion (BIC) for a fitted MFA or MPCA
model. This is a unified wrapper that dispatches to the appropriate
method-specific function.
}
\details{
BIC is computed as \eqn{-2 \log L + p \log(N)}, where L is the likelihood,
p is the number of parameters, and N is the sample size.
}
\examples{
\dontrun{
fit <- mfa_em_fit(list_of_data, K = 3, r = 2)
N_obs <- sum(sapply(list_of_data, nrow))
M <- ncol(list_of_data[[1]])
bic <- compute_bic_wrapper("MFA", fit, K = 3, r = 2, M = M, N_obs = N_obs)
}

}
