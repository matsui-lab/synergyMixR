% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_cluster_synergy_loadings_mpca}
\alias{plot_cluster_synergy_loadings_mpca}
\title{Plot Cluster-Specific Synergy Coefficients (Factor Loadings) for MPCA}
\usage{
plot_cluster_synergy_loadings_mpca(
  mpca_fit,
  cluster_ids = NULL,
  plot_type = c("heatmap", "bar"),
  muscle_names = NULL
)
}
\arguments{
\item{mpca_fit}{A fitted MPCA model object containing \code{W}, \code{z}, etc.}

\item{cluster_ids}{A numeric vector of cluster IDs to plot. Defaults to all.}

\item{plot_type}{One of \code{"heatmap"} or \code{"bar"}. Specifies how to visualize.}

\item{muscle_names}{Optional character vector of muscle names for y-axis labels.
If NULL, default channel names will be used.}
}
\value{
A \code{ggplot} object (which you can print or further modify).
}
\description{
This function takes a fitted MPCA model (e.g. from \code{mixture_pca_em_fit()})
and produces a faceted heatmap or bar plot of each cluster's factor loadings
\code{W[[k]]} (an \code{M x r} matrix).
}
\examples{
\dontrun{
  # Suppose we have a best MPCA model in 'best_mpca_model'
  # that has best_mpca_model$W[[k]] for each cluster
  p <- plot_cluster_synergy_loadings_mpca(best_mpca_model, cluster_ids = 1:2)
  print(p)
}
}
