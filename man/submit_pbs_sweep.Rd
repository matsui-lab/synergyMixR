% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pbs_utils.R
\name{submit_pbs_sweep}
\alias{submit_pbs_sweep}
\title{Submit PBS Parameter Sweep}
\usage{
submit_pbs_sweep(
  param_grid,
  output_dir,
  pbs_config = list(),
  sweep_type = c("modelsel", "baselines"),
  sweep_config = list(),
  submit = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{param_grid}{Data frame. Parameter grid to sweep.}

\item{output_dir}{Character. Directory for output files.}

\item{pbs_config}{List. PBS configuration options (see \code{\link{generate_pbs_script}}).}

\item{sweep_type}{Character. Type of sweep: "modelsel" or "baselines".}

\item{sweep_config}{List. Sweep-specific configuration.}

\item{submit}{Logical. If TRUE, submit the job. If FALSE, only generate scripts.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
List containing:
  \itemize{
    \item \code{job_id}: PBS job ID (if submitted)
    \item \code{script_file}: Path to the PBS script
    \item \code{param_grid_file}: Path to the saved parameter grid
    \item \code{output_dir}: Output directory
    \item \code{n_tasks}: Number of tasks
  }
}
\description{
Prepares and optionally submits a PBS array job for parameter sweep.
}
\examples{
\dontrun{
# Generate scripts only (dry run)
result <- submit_pbs_sweep(
  param_grid = make_param_grid_test(),
  output_dir = "results",
  submit = FALSE
)

# Submit job
result <- submit_pbs_sweep(
  param_grid = make_param_grid_paper_core(),
  output_dir = "results",
  pbs_config = list(queue = "batch"),
  submit = TRUE
)
}

}
