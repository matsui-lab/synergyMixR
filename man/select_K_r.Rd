% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_selection_wrappers.R
\name{select_K_r}
\alias{select_K_r}
\title{Unified Model Selection for K and r}
\usage{
select_K_r(
  list_of_data,
  method = c("MFA", "MPCA"),
  strategy = c("grid", "twostage"),
  Kvec = 1:5,
  rvec = 1:5,
  r_fixed = 2,
  criterion = c("ICL", "BIC"),
  icl_max_iter = NULL,
  icl_temperature = 2,
  max_iter = 50,
  nIterFA = 5,
  nIterPCA = 5,
  tol = 0.001,
  mpca_method = "EM",
  n_init = 1,
  use_kmeans_init = TRUE,
  subject_rdim_for_kmeans = 2,
  mc_cores_grid = 1,
  mc_cores = 1,
  n_threads = 1,
  verbose = TRUE
)
}
\arguments{
\item{list_of_data}{A list of data matrices, each (T_i x M).}

\item{method}{Model type: "MFA" or "MPCA".}

\item{strategy}{Selection strategy: "grid" for full grid search, "twostage" for
two-stage sequential selection (K first, then r).}

\item{Kvec}{Integer vector of candidate K values. Default is 1:5.}

\item{rvec}{Integer vector of candidate r values. Default is 1:5.}

\item{r_fixed}{Fixed r value for Stage 1 of twostage selection. Default is 2.}

\item{criterion}{Model selection criterion: "ICL" (Integrated Completed Likelihood)
or "BIC" (Bayesian Information Criterion). Default is "ICL".}

\item{icl_max_iter}{Maximum iterations for ICL entropy calculation. Default is NULL
(uses package default).}

\item{icl_temperature}{Temperature parameter for ICL entropy scaling. Higher values
favor more clusters. Default is 2.0.}

\item{max_iter}{Maximum EM iterations. Default is 50.}

\item{nIterFA}{Sub-iterations for FA update (MFA only). Default is 5.}

\item{nIterPCA}{Sub-iterations for PCA update (MPCA only). Default is 5.}

\item{tol}{Convergence tolerance. Default is 1e-3.}

\item{mpca_method}{Method for MPCA: "EM" or "closed_form". Default is "EM".}

\item{n_init}{Number of random initializations. Default is 1.}

\item{use_kmeans_init}{Whether to use k-means initialization. Default is TRUE.}

\item{subject_rdim_for_kmeans}{Dimension for k-means features. Default is 2.}

\item{mc_cores_grid}{Number of cores for grid-level parallelization. Default is 1.}

\item{mc_cores}{Number of cores for initialization-level parallelization. Default is 1.}

\item{n_threads}{Number of OpenMP threads. Default is 1.}

\item{verbose}{Logical; if TRUE, print progress messages. Default is TRUE.}
}
\value{
A list with unified output structure:
\describe{
  \item{\code{best_K}}{Optimal number of clusters (scalar).}
  \item{\code{best_r}}{Optimal factor dimension (scalar).}
  \item{\code{best_model}}{Fitted model object with optimal (K, r).}
  \item{\code{summary_df}}{Data frame with selection summary. For grid strategy,
    contains all (K, r) combinations with BIC. For twostage, contains stage1 and
    stage2 summaries.}
  \item{\code{meta}}{List with metadata: method, strategy, Kvec, rvec, etc.}
}
}
\description{
This function provides a unified interface for selecting optimal K (number of
clusters) and r (factor dimension) for mixture models (MFA or MPCA). It supports
multiple selection strategies including grid search and two-stage selection.
}
\details{
This function wraps the existing model selection functions to provide a unified
interface for parameter sweep experiments. It handles:
\itemize{
  \item Parallel safety: Prevents nested parallelization by forcing n_threads=1
    when mc_cores_grid > 1
  \item Windows compatibility: Falls back to sequential execution on Windows
  \item Consistent output format: Returns the same structure regardless of
    method or strategy
}

\strong{Strategy: "grid"}
Performs full grid search over all (K, r) combinations using BIC for selection.
This is thorough but computationally expensive.

\strong{Strategy: "twostage"}
Two-stage sequential selection:
\enumerate{
  \item Stage 1: Fix r at r_fixed, search for optimal K using BIC
  \item Stage 2: Fix K at optimal value, search for optimal r using BIC
}
This is more stable and faster than full grid search.
}
\examples{
\dontrun{
# Grid search for MFA
result <- select_K_r(
  list_of_data, method = "MFA", strategy = "grid",
  Kvec = 2:4, rvec = 2:4
)
print(result$best_K)
print(result$best_r)

# Two-stage selection for MPCA
result <- select_K_r(
  list_of_data, method = "MPCA", strategy = "twostage",
  Kvec = 2:4, rvec = 2:4, r_fixed = 2
)
}

}
\seealso{
\code{\link{select_optimal_K_r_mfa}}, \code{\link{select_optimal_K_r_mpca}},
  \code{\link{select_optimal_K_r_mfa_twostage}}, \code{\link{select_optimal_K_r_mpca_twostage}}
}
