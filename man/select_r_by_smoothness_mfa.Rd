% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r_selection_advanced.R
\name{select_r_by_smoothness_mfa}
\alias{select_r_by_smoothness_mfa}
\title{Select Optimal r by Smoothness Penalty for MFA}
\usage{
select_r_by_smoothness_mfa(
  list_of_data,
  K,
  rvec = 1:6,
  lambda_smooth = 0.001,
  vaf_threshold = 0.9,
  max_iter = 50,
  nIterFA = 5,
  tol = 0.001,
  n_init = 1,
  use_kmeans_init = TRUE,
  subject_rdim_for_kmeans = 2,
  mc_cores = 1,
  n_threads = 1
)
}
\arguments{
\item{list_of_data}{A list of N matrices, each (T_i x M).}

\item{K}{Number of clusters (fixed).}

\item{rvec}{Vector of candidate r values to evaluate.}

\item{lambda_smooth}{Smoothness penalty weight. Default is 1e-3.}

\item{vaf_threshold}{Minimum VAF threshold. Models with VAF below this are excluded.
Default is 0.9.}

\item{max_iter}{Maximum EM iterations.}

\item{nIterFA}{Sub-iterations for FA update.}

\item{tol}{Convergence tolerance.}

\item{n_init}{Number of random initializations.}

\item{use_kmeans_init}{Whether to use k-means initialization.}

\item{subject_rdim_for_kmeans}{Dimension for k-means features.}

\item{mc_cores}{Number of cores for parallelization.}

\item{n_threads}{Number of OpenMP threads.}
}
\value{
A list with:
  \item{best_r}{The selected r value.}
  \item{summary}{A data.frame with columns r, BIC, VAF, smoothness, penalized_score.}
  \item{best_model}{The fitted model for the selected r.}
}
\description{
Selects the optimal number of factors (r) for a given number of clusters (K)
by minimizing the smoothness penalty of factor activation profiles, subject
to constraints on VAF or BIC.
}
\details{
This function fits MFA models for each r in rvec with fixed K, computes
factor scores, and evaluates the smoothness penalty. The optimal r is selected
by minimizing:
\deqn{
  \text{score} = \text{BIC} + \lambda_{\text{smooth}} \times \text{smoothness}
}
among models satisfying VAF >= vaf_threshold.
}
\examples{
\dontrun{
result <- select_r_by_smoothness_mfa(
  list_of_data, K = 3, rvec = 1:5,
  lambda_smooth = 1e-3, vaf_threshold = 0.9
)
print(result$summary)
best_model <- result$best_model
}

}
