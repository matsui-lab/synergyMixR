% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_synergy.R
\name{plot_cluster_embedding}
\alias{plot_cluster_embedding}
\title{Plot Cluster Embedding}
\usage{
plot_cluster_embedding(sim, fit = NULL, method = "umap", theme_base_size = 14)
}
\arguments{
\item{sim}{A list returned by \code{simulate_dynamic_synergy_data()}.
Must contain \code{X_list} and \code{z_true} components.}

\item{fit}{Optional fitted model object (e.g., from MFA/MPCA). If provided,
predicted cluster assignments can be overlaid. Default is NULL.}

\item{method}{Character string specifying the embedding method: "umap" or
"pca". Default is "umap".}

\item{theme_base_size}{Base font size for the plot theme. Default is 14.}
}
\value{
A ggplot object showing low-dimensional embedding of subjects.
}
\description{
Creates a low-dimensional embedding (UMAP or PCA) to visualize cluster
separability. Each point represents a subject, colored by true cluster
assignment.
}
\examples{
\dontrun{
sim <- simulate_dynamic_synergy_data(N=30, K=3, r=3, M=8, T_each=200)
plot_cluster_embedding(sim, method = "umap")
plot_cluster_embedding(sim, method = "pca")
}

}
